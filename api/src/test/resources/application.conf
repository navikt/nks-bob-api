ktor {
  deployment {
    port = 8080
  }
  application {
    modules = [no.nav.nks_ai.api.ApplicationKt.module]
  }
  openApi {
    enabled = false
  }
}

kbs {
  url = "http://localhost:1080"
  scope = "scope"
}

vaskemaskin {
  url = "http://localhost:8585"
  scope = "scope"
}

jwt {
  clientId = "test-client-id"
  clientSecret = "test-client-secret"
  configTokenEndpoint = "http://localhost:9090/token"
  adminGroup = "test-admin-group"
  adminGroup = ${?TEST_ADMIN_GROUP}
}

db {
  username = "test"
  username = ${?DB_USERNAME}
  password = "test"
  password = ${?DB_PASSWORD}
  database = "nks_bob_test"
  host = "localhost"
  port = "5432"
  jdbcURL = ""
  jdbcURL = ${?DB_JDBC_URL}
}

nais {
  electorUrl = ""
  appName = ""
  tokenEndpoint = "http://localhost:3333/api/v1/token"
  preAuthorizedApps = ""
  corsExtraOrigins = ""
}

unleash {
  serverApiUrl = ""
  serverApiToken = ""
  appName = "nks-bob-api-test"
}

metrics {
  navIdentSecret = "test-secret"
}

issuer = {
  issuer_name = "test-issuer"
  issuer_name = ${?TEST_ISSUER_NAME}
  discoveryurl = "http://localhost:9090/test-issuer/.well-known/openid-configuration"
  discoveryurl = ${?TEST_ISSUER_DISCOVERY_URL}
  jwksurl = "http://localhost:9090/test-issuer/jwks"
  jwksurl = ${?TEST_ISSUER_JWKS_URL}
  accepted_audience = "test-audience"
  accepted_audience = ${?TEST_ISSUER_ACCEPTED_AUDIENCE}
}

bigquery {
  projectId = "local"
  kunnskapsbaseDataset = "kunnskapsbase"
  kunnskapsartiklerTable = "kunnskapsartikler"
  testgrunnlagDataset = "testgrunnlag"
  stjernemarkerteSvarTable = "stjernemarkerte_svar_local"
}
